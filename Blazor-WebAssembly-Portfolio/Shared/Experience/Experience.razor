@namespace BlazorWebAssemblyPortfolio.Shared.Experience
<MudTimeline id="experience">
    @foreach(var item in config.Items)
    {
         <ExperienceItem Body="@item.Description" Date="@item.Date" Heading="@item.Company" Subheading="@item.Role"></ExperienceItem>
    }
</MudTimeline>

